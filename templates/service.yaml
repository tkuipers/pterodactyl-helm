apiVersion: v1
kind: Service
metadata:
  name: {{ include "pterodactyl-helm.fullname" . }}
  labels:
    {{- include "pterodactyl-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pterodactyl-helm.selectorLabels" . | nindent 4 }}
